services:
  postgres:
    image: postgres:17

    container_name: dojo-db

    restart: always

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: dojo

    ports:
      - '5432:5432'

    volumes:
      - postgres_data:/var/lib/postgresql/data

  pgadmin:
    image: dpage/pgadmin4
    container_name: dojo-pgadmin
    restart: always

    environment:
      PGADMIN_DEFAULT_EMAIL: admin@dojo.com
      PGADMIN_DEFAULT_PASSWORD: admin123

    ports:
      - '8080:80'

    depends_on:
      - postgres

volumes:
  postgres_data:
